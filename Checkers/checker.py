import math;
class Checker:
	
	def __init__(self, color):
		""" This method intitiates the color of the checker """
		self.color = color;
		self.alive = True;
		self.position = (0,0);
		self.king = False;
	def move(pos, later):
		
		
		
	def slope(pos, later):
		 
		slope = pos[1] - 
	
